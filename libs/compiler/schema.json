{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "appRoot": {
      "type": "string",
      "description": "Path to the app's root config.js"
    }
  },
  "required": ["appRoot"]
}
